---
'@you-want/polyllm-cli': minor
---

Added the `polyllm studio` command. It starts the bundled visual configuration UI and local model-probing API without requiring users to clone the repository or run Vite.
